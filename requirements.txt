# AceIt dependencies (Windows). OCR uses Tesseract via pytesseract — no EasyOCR / python-bidi.
#
# One-time: install Tesseract for Windows (see INSTALL_WINDOWS.txt), then:
#   python -m pip install -r requirements.txt
#
# If you previously used google-generativeai: python -m pip uninstall -y google-generativeai
#
pyautogui>=0.9.54
Pillow>=10.0.0
pytesseract>=0.3.10
google-genai>=1.0.0
python-dotenv>=1.0.0
keyboard>=0.13.5
