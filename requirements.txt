# Atlas dependencies (Windows)
#
# One-time: install Tesseract for Windows (see INSTALL_WINDOWS.txt), then:
#   python -m pip install -r requirements.txt
#
pyautogui>=0.9.54
Pillow>=10.0.0
pytesseract>=0.3.10
google-generativeai>=0.8.0
python-dotenv>=1.0.0
keyboard>=0.13.5
groq>=0.9.0
numpy>=1.24.0
sounddevice>=0.4.6
soundfile>=0.12.1
webrtcvad>=2.0.10
PySide6>=6.6.0
kokoro-onnx
opencv-python
mss
pyperclip
markdown-it-py
pymupdf
# Optional premium streaming TTS — used only when ELEVENLABS_API_KEY + VOICE_ID
# are set; Atlas falls back to offline Kokoro automatically when absent.
elevenlabs
# Optional cloud accounts + sync — used only when SUPABASE_URL + SUPABASE_ANON_KEY
# are set; Atlas runs fully local-first (offline) when absent.
supabase
# Learn-and-Execute demonstration recorder (global mouse/keyboard capture).
pynput>=1.7.6
# Chat export to PDF.
reportlab>=4.0.0
# TOTP authenticator app 2FA.
pyotp>=2.9.0
# Guided-mode doc lookup ([[RESEARCH:]] grounding).
trafilatura>=1.6.0
requests>=2.31.0
duckduckgo-search>=6.0.0
# Connector token encryption + Windows DPAPI key protection.
cryptography>=42.0.0
pywin32>=306
# Background daemon IPC (localhost-only HTTP + WebSocket).
fastapi>=0.110.0
uvicorn>=0.27.0
websocket-client>=1.7.0
# Cron scheduling with SQLite job-store persistence (survives daemon restarts).
APScheduler>=3.10.0
SQLAlchemy>=2.0.0
