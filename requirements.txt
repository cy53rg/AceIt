# AceIt dependencies (Windows). OCR uses Tesseract via pytesseract — no EasyOCR / python-bidi.
#
# One-time: install Tesseract for Windows (see INSTALL_WINDOWS.txt), then:
#   python -m pip install -r requirements.txt
#
# Legacy Gemini SDK (stable for older model ids). If you had google-genai: pip uninstall -y google-genai
#
pyautogui>=0.9.54
Pillow>=10.0.0
pytesseract>=0.3.10
google-generativeai>=0.8.0
python-dotenv>=1.0.0
keyboard>=0.13.5
